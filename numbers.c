/*
 * Assignment 1: Getting Your Feet Wet in C
 *
 * Fill in the parts marked TODO. Do NOT add your own functions — write all of
 * your code inside main(). You will use variables, types, a loop (iteration),
 * and if/else (selection).
 *
 * When finished, compile and run:
 *     gcc -Wall numbers.c -o numbers
 *     ./numbers
 */

#include <stdio.h>

int main(void) {

    /* ---- Part A: sizes of types ----------------------------------------
     * Print the size in bytes of an int and of a double, exactly like this
     * (the numbers depend on the machine; use sizeof, don't hard-code them):
     *
     *     int size: 4
     *     double size: 8
     *
     * Use sizeof and the %zu format specifier to print the sizes.
     * TODO: write two printf statements below.
     */



    /* ---- Part B: count from 1 to 10 with labels ------------------------
     * Using a LOOP, print the numbers 1 through 10, one per line. For each
     * number, also print a label chosen with if/else:
     *   - if the number is even, print:  N even
     *   - if the number is odd,  print:  N odd
     * where N is the number. For example the first three lines are:
     *
     *     1 odd
     *     2 even
     *     3 odd
     *
     * TODO: write a for-loop with an if/else inside it below.
     */



    return 0;
}
